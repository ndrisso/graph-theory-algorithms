class BellmanFord
  attr_accessor :graph, :source, :destination, :distances

  def initialize(graph, source, destination)
    self.graph       = graph
    self.source      = source
    self.destination = destination
  end

  def apply

  end
end
